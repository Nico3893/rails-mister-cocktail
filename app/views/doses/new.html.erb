<h1>Add dose to <%= @cocktail.name %></h1>
<%= simple_form_for([@cocktail, @dose]) do |f| %>
<%= f.input :description %>
<%= f.association :ingredient %>
<%= f.input :cocktail_id, as: :hidden, value: @cocktail.id %>
<%= f.submit "Add your Dose", class: "btn btn-primary" %>
<% end %>
